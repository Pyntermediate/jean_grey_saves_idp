import 'package:flutter/material.dart';
import 'real_emergency_screen.dart';

class RealDispatchScreen extends StatelessWidget {
  const RealDispatchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const RealEmergencyScreen();
  }
}
